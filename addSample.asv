function addSample(depth)
    global samplesList;
    global samplesTable;
    [x,y] = ginput;
    for i:1:length(x)
    depthList(i 
    end
    disp(depth(round(y),round(x)))
    samplesList(1:3,end+1) = [x';y'; depth(round(y),round(x))];
    set(samplesTable, 'Data', samplesList);
end

